import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  setBtn: {
    position: 'absolute',
    width: '100%',
    height: '100%',
    top: '0',
    left: '0',
  },
}));

export default useStyles;
