import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  routerMenuLink: {
    textDecoration: 'none',
    color: '#000000DE',
    width: '100%',
    padding: '6px 16px',
  },
  routerMenuItem: {
    padding: '0',
  },
});

export default useStyles;
